import React from "react";
import Section from "../components/Section";

function About({ id, style }) {
  return (
    <Section id={id} style={style}>
      <h1>About Me</h1>
    </Section>
  );
}

export default About;
