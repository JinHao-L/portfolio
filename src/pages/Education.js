import React from "react";
import Section from "../components/Section";

function Education({ id, style }) {
  return (
    <Section id={id} style={style}>
      <h1>I am studying in NUS</h1>
    </Section>
  );
}

export default Education;
